# A500 ETF 量化工具 需求说明

## 问题

手动跟踪 A500 ETF（563360）和相关 ETF 的 K 线走势费时费力，容易被盘中噪音、新闻和情绪影响。需要一个本地工具获取数据、计算技术指标、展示关键位置，并回测 RSI 低位买入信号的历史质量，辅助达达做中线波段判断。

## 交易哲学

详见 `PROJECT_CONTEXT.md`。核心原则：

- **弱者体系**：工具整理信息，不替人赚钱。
- **极端交易**：只在 RSI 进入低位区间时重点关注。
- **简单有效**：使用可解释的技术指标，不使用 ML/DL。
- **低频高质**：信号稀缺是优势。
- **卖出自主**：工具不提供卖出策略，卖出由达达自己判断。

## 用户与场景

达达（个人投资者）在桌面电脑上运行命令行工具或 Streamlit 图形界面。主页面在十秒内呈现状态、关键数字、下一步观察和图表；复盘、战役回测和规则按需分页面查看。

## MVP 范围

- 从 akshare 获取 ETF 日 K 线数据。
- 本地缓存数据，避免重复请求。
- 支持标的：563360 A500 ETF、510300 沪深300 ETF、518880 黄金 ETF、588000 科创50 ETF。
- 上述四只为正式策略区；恒生、港股个股和原油等扩展标的仅可在实验观察区查看行情与指标，不展示战役条件或策略回测。
- 使用本地 pandas/numpy 实现计算 MA5/MA10/MA20/MA60、MACD、RSI(14)、ADX(14)、布林带(20,2)。
- 终端输出决策辅助面板。
- Streamlit 输出状态图表、复盘日志、完整战役回测和策略规则页面。
- 用 matplotlib 绘制四层图表：价格、RVOL、MACD、RSI。

## 三段战役观察规则

## 看盘顺序

1. 价格位置：收盘价在 MA5/MA10/MA20 上方、下方，或夹在均线之间。
2. RVOL：固定用 `成交额 / 20日成交额均线` 判断价格变化是否有资金确认；没有成交额数据时 RVOL 暂不可用，不回退成交量。
3. MACD：用 DIF、DEA 和柱状图判断趋势动能增强或衰弱。
4. RSI：最后判断是否过热、是否进入低位观察区。

RVOL 和 MACD 只作为观察辅助；正式 RVOL 必须来自可验证成交额，成交量或估算额均显示为不可用。

## 面板解释结构

主页面和 CLI 使用同一套状态化分析逻辑。主页面默认只输出当前状态、一句话解释、最多三条下一步观察和图表；详细四步解释收进折叠区。

1. 当前状态：强势放量突破、缩量偏强、健康回踩、高位过热、放量破位警戒、弱势反弹或中性观察。
2. 一句话解释：把价格、RVOL、MACD、RSI 合成一段新手可读的观察结论。
3. 看盘顺序：严格按价格位置、RVOL、MACD、RSI 四步解释。
4. 战役状态：只显示第一观察位、第二观察位和右侧确认的事实状态。
5. 下一步观察：提示下一交易日重点看什么。

文案边界：工具不输出买入、加仓、止盈、止损、清仓、减仓或卖出指令；所有战役条件均为人工复核事实。

看盘解释模块只判断当前结构状态，不默认套用 RSI 低位现金池买入逻辑。RSI 现金池计划只在 RSI 进入低位观察区时，作为单独纪律提醒出现。

`缩量偏强` 状态强调趋势偏强、MACD偏多、RSI正常但 RVOL 偏低，适合持有观察、不直接追涨，后续重点看能否放量突破（RVOL > 1.2）或回踩 MA5 / MA10 时缩量不破；RVOL 仍只作为观察辅助，不作为硬性买入条件。

### 分段阈值与右侧确认

| ETF 类别 | 第一观察位 | 第二观察位 | 第三段人工复核 |
|---|---:|---:|---|
| 宽基股票 ETF | RSI ≤ 40 | RSI ≤ 35 | RSI ≥ 40 且 MACD 柱改善 |
| 黄金 ETF | RSI ≤ 35 | RSI ≤ 30 | RSI ≥ 40 且 MACD 柱改善 |
| 科创50 ETF | RSI ≤ 30 | RSI ≤ 25 | RSI ≥ 40 且 MACD 柱改善 |

总预算、批次和条件在战役开始前锁定；价格变化不增加预算或批次。第三段未确认时，剩余现金继续等待。

## 卖出边界

- 工具不提供卖出策略。
- 不输出止盈、止损、清仓、减仓或卖出信号。
- RSI>70、ADX、均线、布林带都只作为状态信息，不解释为卖出指令。
- 卖出由达达结合市场背景、资金用途、持仓计划和个人风险承受能力自己判断。

## 回测要求

- 回测对象是完整三段战役，不包含卖出策略。
- 首先识别第一观察位、第二观察位和右侧确认的完整顺序；尚未完成确认的战役单独标为等待，不计入收益统计。
- 三段按预设等权成本计算组合收益；在右侧确认后的 20、60、120、250 个交易日统计上涨概率、平均收益、中位收益、最差和最好收益。

## 输入与输出

- 输入：ETF 代码、图表展示天数、是否强制刷新。
- 输出：终端面板、本地图表、Streamlit 状态图表、复盘日志、完整战役回测表和规则说明。

## 验收标准

- [ ] 运行 `.venv\Scripts\python.exe main.py --no-plot` 可打印最新面板。
- [ ] 支持 `--symbol 518880`，黄金 ETF 使用 35/30 两段观察位。
- [ ] 支持 `--symbol 588000`，科创50 ETF 使用 30/25 两段观察位。
- [ ] 支持 `--days 90` 等参数调整图表窗口。
- [ ] K 线图按价格、RVOL、MACD、RSI 四层展示。
- [ ] 面板按价格位置、RVOL、MACD、RSI 顺序输出提示。
- [ ] Streamlit 主页面显示当前状态、关键数字、最多三条下一步观察和图表；复盘、回测、规则为独立页面。
- [ ] MACD 统一显示为 DIF / DEA / HIST，动能判断按 HIST 与前一日 HIST 比较。
- [ ] 面板关键数字显示 RSI、MACD、成交量、成交额和 RVOL；RVOL 缺少成交额时显示暂不可用。
- [ ] Streamlit 回测只统计完整三段战役，使用等权三段成本。
- [ ] 项目不输出卖出策略或自动买卖信号。

## 风险与依赖

- akshare 接口可能因上游变动而失效，需预留备用数据源。
- 东方财富日 K 接口不可用时，自动切换到新浪 ETF 日 K 接口；若两个接口都失败，才回退本地缓存并显示提示。
- 所有信息仅为辅助参考，不构成投资建议。
- RSI 低位策略可能长时间没有有效信号，需要接受“等待”本身是策略的一部分。
